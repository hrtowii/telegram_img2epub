{
  "name": "telegram_img2epub",
  "module": "index.ts",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "type": "module",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.24.3",
    "https": "^1.0.0",
    "libgen": "^2.2.0",
    "telegraf": "^4.16.3",
    "telegraf-pagination": "^1.4.1"
  },
  "scripts": {
    "start": "bun index.ts",
    "build": "docker build --pull -t $(basename $(pwd)) ."
  }
}
